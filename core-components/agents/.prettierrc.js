module.exports = {
  // ============================================================
  // 通用格式化选项
  // ============================================================

  // 每行最大字符数，超过会自动换行。默认 80。增大可减少换行，减小可提高可读性。
  printWidth: 80,

  // 缩进使用的空格数。默认 2。常见值为 2 或 4。
  tabWidth: 2,

  // 是否使用 Tab 代替空格缩进。默认 false。若为 true，缩进将使用 tabWidth 宽度的制表符。
  useTabs: false,

  // ============================================================
  // 分号与引号
  // ============================================================

  // 是否在语句末尾添加分号。默认 true。设为 false 仅在 ASI 安全时才省略分号。
  semi: true,

  // 是否使用单引号代替双引号。默认 false。JSX 中的引号受 jsxSingleQuote 单独控制。
  singleQuote: false,

  // 对象字面量中属性名是否加引号。默认 "as-needed"。
  // - "as-needed": 仅在需要时加引号
  // - "consistent": 若对象中任一属性需要引号，则所有属性都加
  // - "preserve": 保持输入时的引号状态不变
  quoteProps: 'as-needed',

  // JSX 中是否使用单引号。默认 false。独立于 singleQuote，仅影响 JSX 属性。
  jsxSingleQuote: false,

  // ============================================================
  // 尾随逗号
  // ============================================================

  // 多行结构末尾是否添加尾随逗号。默认 "all" (v2.0+ 起)。
  // - "none": 不添加尾随逗号
  // - "es5": 在 ES5 有效位置添加（对象、数组等）
  // - "all": 在所有可能位置添加（包括函数参数和调用）
  trailingComma: 'all',

  // ============================================================
  // 括号与空格
  // ============================================================

  // 对象字面量大括号内侧是否加空格 { foo: bar } vs {foo: bar}。默认 true。
  bracketSpacing: true,

  // JSX 多行元素的闭合 > 是否与最后一行属性放在同一行。默认 false。
  // false: > 另起新行；true: > 放在最后一行末尾。
  bracketSameLine: false,

  // 箭头函数唯一参数是否加括号。默认 "always"。
  // - "always": 始终加括号 (x) => x
  // - "avoid": 能省略时省略括号 x => x
  arrowParens: 'always',

  // ============================================================
  // HTML / Vue / JSX 相关
  // ============================================================

  // HTML 中空格的敏感度。默认 "css"。
  // - "css": 按 CSS display 值处理（默认，最常用）
  // - "strict": 所有标签内空白视为有意义
  // - "ignore": 所有标签内空白视为无意义
  htmlWhitespaceSensitivity: 'css',

  // 是否缩进 Vue SFC 中 <script> 和 <style> 标签的内容。默认 false。
  vueIndentScriptAndStyle: false,

  // 是否强制每个 HTML/JSX 属性独占一行（当元素有多个属性时）。默认 false。
  singleAttributePerLine: false,

  // ============================================================
  // 特殊文件类型
  // ============================================================

  // Markdown 文本的换行方式。默认 "preserve"。
  // - "always": 超过 printWidth 就换行
  // - "never": 不自动换行，保留原文
  // - "preserve": 尊重原始换行（默认，适合 Markdown）
  proseWrap: 'preserve',

  // ============================================================
  // 格式化控制
  // ============================================================

  // 是否只格式化文件顶部有 @prettier 或 @format 标记的文件。默认 false。
  requirePragma: false,

  // 格式化后是否在文件顶部插入 @format 标记。默认 false。
  insertPragma: false,

  // 是否格式化嵌入在其他文件中的代码（如 Markdown 中的代码块、HTML 中的 <script>）。
  // 默认 "auto"。设为 "off" 可跳过嵌入代码的格式化。
  embeddedLanguageFormatting: 'auto',

  // ============================================================
  // 行尾与文件编码
  // ============================================================

  // 行尾换行符类型。默认 "lf"。
  // - "lf": Unix/macOS 风格 (\\n)
  // - "crlf": Windows 风格 (\\r\\n)
  // - "cr": 仅回车符 (\\r)
  // - "auto": 自动检测并保持现有行尾风格
  endOfLine: 'lf',
};
